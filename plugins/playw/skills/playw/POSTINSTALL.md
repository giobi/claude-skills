# Post-Install: /playw

## Check:

1. Run `npx playwright --version` — if it fails:
   "Playw needs Playwright. Install with: `npm init -y && npx playwright install chromium`"

## Then:

Create `wiki/skills/playw.md`:

```yaml
---
type: skill-config
playwright: true
tags:
  - skill
  - playw
  - testing
---

# /playw configuration

Playwright: installed
```
